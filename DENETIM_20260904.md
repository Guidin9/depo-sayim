# Bağımsız denetim — 2026-09-04

**Kapsam:** `app/` (6.097 satır) + `web/src/` (8.560 satır), dokümanlara
güvenmeden sıfırdan okuma. Her iddia gerçek Tiger verisi (`deneme.XLSX`,
Ambar 1, 870 satır) ve canlı `sayim.db` kopyası üzerinde **üretildi** —
hiçbiri okuma sonucu tahmin değildir.

**Denetim anındaki durum:** 478 arka uç + 32 arayüz testi geçiyor,
`npm run build` temiz. **Aşağıdaki bulguların hiçbirini test paketi
yakalamıyor.** 2026-08-27 (8 hata) ve 2026-09-02 (5 hata) denetimlerinin
dersinin aynısı: yeşil test paketi "hata yok" demek değildir.

**Yedek doğrulandı** (2026-09-04): `data/sayim.db`, `data/yedek-20260902-…/`
ve `%USERPROFILE%\Desktop\depo-sayim-yedek-20260902\` üçü de SHA256
`18a0ec04810906cd…` — 28.08'den beri canlı veri değişmemiş, 02.09 yedeği
hâlâ güncel.

---

## Bu denetimin ortak teması

Üç düzeltmenin **yarısı uygulanmış**: kural bir dalda kapatılmış, aynı kuralı
kullanan öteki dallara taşınmamış.

| Kapatılan hata | Düzeltme nereye kondu | Nereye konmadı |
|---|---|---|
| B2 (UPC seri no önerilir) | `matching._sn_karar` | `reports._yeni_seri` tek parçalı dal → K1 |
| B4 (sayaç satır bazlı) | `sayaclar`, `ara`, `eksik_lotlar` | `oturumlar.gecmis` → Y2 |
| D1 (seri satırında miktar>1) | `beklenen_adet`, `kapasite_kaldi` | `slot` sorgusu, `coz()` 5. adım → Y1 |

Dördüncüsü ters yönde: **S2 düzeltmesi yeni bir sessiz çift sayım açtı** (K2).

---

## KRİTİK — sessiz yanlış sayım

### K1 · UPC yeniden Tiger'a seri numarası olarak öneriliyor (B2 geri gelmiş)

- [x] **KAPATILDI** (2026-09-04)

**Yer:** `reports._yeni_seri`

```python
parcalar = [p for p in parcalar if etiket_turu(p) != "kutu"]   # kap elemesi taşınmış
if not parcalar:
    return ""
if len(parcalar) == 1:
    return parcalar[0]                    # <-- UPC elemesi BURADAN SONRA
adaylar = [p for p in parcalar if not upc_mi(p)] or parcalar
```

Kap kodu elemesi bilerek yukarı taşınmış ve yorumu bunu söylüyor
(*"Eleme TEK PARÇALI girdide de yapılmalı"*). UPC elemesi taşınmamış.

**Üretildi:**

```
reports._yeni_seri('198701689928')                 -> '198701689928'
reports._yeni_seri('X + 198701689928')             -> 'X'
matching._fazla_seri(['0WGP72','198701689928'], '0WGP72') -> '198701689928'

UPC tek başına okutuldu -> kuyruk -> kirli slota çözüldü:
Tiger Düzeltme: ['BC-U6030', 'BEEK PATCH CORD CAT 6 UTP 3MT',
                 'BC-U6030SAYIM1', '198701689928', '', ...]
```

B2'nin düzeltmesi (`_sn_karar`) yalnızca `grup_coz`'un okutma dalında.
`_fazla_seri`'ye dayanan **dört yol korumasız**: `kuyruk_coz`, `fazla_bagla`,
`kuyruk_fazla`, `##FAZLA##` komutu.

Rapordaki "son savunma" da tutmuyor: `kirli_mi("198701689928", kod)` **temiz**
döner (rakam, boşluk yok, kodla başlamıyor, 12 < 25).

**Canlı veride hazır bekliyor** — Eşleştirme ekranı sıradaki adım ve K1 tam
orada tetikleniyor:

| Okutma | `seri` | `ham` |
|---|---|---|
| #22 | `5099206004023` | `5099206004023` |
| #92 | `8697939919651` | `DK-000002 + 8697939919651` |

**Neden kritik:** perakende barkodu o malzemenin her adedinde aynıdır. Tiger'a
"bu cihazın seri numarası 198701689928" yazmak, 21 cihaza aynı "tekil" numarayı
vermektir — uygulamanın temizlemeye çalıştığı kirliliğin ta kendisi.

---

### K2 · Aynı DS- etiketi ikinci kez okutulunca ikinci bir fazla kaydı doğuyor

- [x] **KAPATILDI** (2026-09-04)

**Yer:** `matching.coz` 1c adımı

```python
e = c.execute("SELECT * FROM etiket WHERE kod=? AND tur='seri'", (n,)).fetchone()
if e:
    if e["beklenen_id"]:      # <-- yalnızca Tiger kaydına bağlı etiket
        ...
    return {"t": "etiket_bos", ...}
```

Fazla yolundan bağlanan etikette `beklenen_id` NULL'dur (Tiger'da kaydı yok)
ama `oturum` **doludur** — yani uygulama etiketin tükendiğini biliyor ve o
bilgiyi hiç okumuyor.

**Üretildi:**

```
DS-000001 + UPC + ##FAZLA##    -> fazla, ad "SUNUCU RAY KİTİ" öğrenildi
DS-000001 + UPC + ##SONRAKI##  -> tip=fazla_bilinen  (ses=uyari, ad hazır)
sonuç: 2 fazla satırı / 2 adet  <- DS- etiketi TEK cihaza ait
kapılar: adsiz=0  fotosuz=0     <- hiçbiri yakalamıyor
```

`##FAZLA##` yolunda ikinci kayıt adsız kalıp `ad_engel`e takılır (kullanıcı
fark edebilir). `fazla_bilinen` yolunda ad zaten öğrenilmiş olduğu için
**tamamen sessiz**: yeşil akış, sarı şerit, hiçbir kapı yok.

**Canlı veride ZATEN OLMUŞ:**

| Okutma | Saat | `ham` | `ad` |
|---|---|---|---|
| #23 | 11:01:01 | `DS-000054` | `*Yanlış Okuma İgnorla` |
| #26 | 11:01:37 | `DS-000054 + 8680096009785` | `SL-75 A` |

Aynı fiziksel etiket, 36 saniye arayla, iki fazla adedi.

**Dikkat — S2 düzeltmesi bunu YAYGINLAŞTIRIYOR.** Çalışma ağacındaki
`fazla_ogren()` artık fazla yolundaki DS- etiketini deftere işliyor
(`oturum` doluyor) ve adı `fazla_ad`'a öğreniyor. Yani sayıma devam edildiğinde
hem etiketin kullanıldığı bilgisi yazılacak hem de ikinci okutma sessiz
`fazla_bilinen` dalına düşecek. Bilgi yeni üretildi, okuyan yok.

**Sınır:** DM- (malzeme) etiketi ya da UPC ikinci kez okutulduğunda ikinci
kayıt DOĞRU olabilir — aynı üründen iki adet olabilir. Kural yalnızca
**DS- seri etiketine** konmalı: o tekil cihaza aittir, iki olamaz.

---

### K3 · Kilit açıkken "Tiger'da yok" bilgisi eziliyor

- [x] **KAPATILDI** (2026-09-04)

**Yer:** `matching.grup_coz`, `fazla_bilinen` dalı `if not kaynak:` içinde.
Kilit `kod_h`'yi doldurup `kaynak`'ı doğru yapıyor, dal hiç çalışmıyor.

**Üretildi:**

```
DM-999999 + ##FAZLA##, ad "SARF KABLO" öğrenildi
04RW5H + ##KILIT##            -> kilit kuruldu
DM-999999 + ##SONRAKI##       -> tip=slot, kod=04RW5H   (yeşil ışık, uyarı yok)
```

Tiger'da kaydı olmadığı **açıkça bilinen** bir kablo, kilitli SQL Server
lisansının kirli slotuna yazıldı.

`eslesme`'ye yazılmıyor (kalıcı bozulma yok) ama sayım yanlış: cihaz yanlış
malzemeye sayıldı, Tiger Düzeltme üretilmedi, kullanıcı yeşil ışık gördü.
Kilit kalıcı bir kip — bir rafta unutulursa bütün raf bu yoldan gider.

**Öncelik sorusu:** `fazla_ad` kullanıcının açık kararıdır ("bu ürün Tiger'da
yok, adı şu"). Kilit bir ortam kipidir. Açık karar, ortam kipini yenmeli.

---

## YÜKSEK

### Y1 · D1 yarım kapatılmış — `slot` ve `coz()` 5 hâlâ eski ölçütte

- [x] **KAPATILDI** (2026-09-04)

`beklenen_adet` her yere taşınmış, ama iki sorgu `SAYILMADI` sabitini
(*hiç okutulmadı*) kullanmaya devam ediyor:

- `matching.grup_coz` → `izleme == "seri"` dalındaki `slot` sorgusu
- `matching.coz` → 5. adım (gömülü S/N içerme)

**Üretildi** (kirli seri satırı, `miktar=2`):

```
1. cihaz -> slot                             (doğru)
2. cihaz -> onay   (kapasite_kaldi=True)     (fazla_onay kuyruğuna düştü)
```

Satır hem 1 adet eksik hem 1 adet fazla_onay olarak raporlanır; kullanıcıya
gereksiz onay sorulur.

**Etkilenen ambarlar:** Ambar 1'de bu satırlardan **yok** (805 seri satırının
hepsi 1 adet). Etkilenen 32 satır ambar 0 (10), 13 (2), 14 (4) ve diğerlerinde
— yani D1'in kendi tespitiyle **tam olarak aynı yerlerde**, sıradaki ambarlarda.

### Y2 · Geçmiş ekranı hâlâ satır bazlı sayıyor (B4 orada kapatılmamış)

- [x] **KAPATILDI** (2026-09-04)

`oturumlar.gecmis()` → `COUNT(DISTINCT beklenen_id)`. Canlı oturum #2:

| Ekran | Okutulan |
|---|---|
| Sayım (`matching.sayaclar`) | **171** |
| Geçmiş (`oturumlar.gecmis`) | **107** |

Aynı oturum, aynı an, iki sayı. B4'ün bütün derdi buydu: ekranla rapor iki ayrı
gerçek söylemesin. `fazla` ve `kuyruk` sayaçları doğru, yalnız `okutulan` eski.

### Y3 · Rapor ekranındaki "Sayımı bitir" düğmesi çıkmaz sokak

- [x] **KAPATILDI** (2026-09-04)

**Yer:** `web/src/App.tsx:102`

```ts
if (mesaj.includes("çözülmemiş") && confirm(`${mesaj}\n\nYine de bitirilsin mi?`))
```

Sunucunun dört kapısı var, yalnız biri bu kelimeyi içeriyor:

| Kapı | Mesaj | "çözülmemiş" | Ele alınıyor mu |
|---|---|---|---|
| kuyruk | "Kuyrukta **çözülmemiş** %d ürün var." | ✔ | ✔ |
| adsız fazla | "%d fazla kaydının ne olduğu yazılmamış." | ✘ | ✘ |
| fotoğrafsız fazla | "%d fazla kaydının fotoğrafı yok." | ✘ | ✘ |
| yumuşak uyarı | "Bitirmeden önce bakın: …" | ✘ | ✘ |

**Üretildi:**

```
77'lik lot bir kez okutuldu
POST /bitir             -> 409  "Bitirmeden önce bakın: 1 lot satırında
                                 sayılan < beklenen"  -> kırmızı kutu, son
POST /bitir?zorla=true  -> 200 bitti          (yol var, arayüz kullanmıyor)
```

Ambar 1'de 69 lot satırı / 271 adet var; sayım sonunda bu kapıya **kesinlikle**
takılacak. Bu kapı belgede açıkça *"ENGEL değil, yumuşak uyarı"* diye tanımlı.
Tek çalışan yol: Sayım ekranında iki kez F10.

Adsız/fotoğrafsız kapıları bilerek serttir — ama orada da mesaj tek başına
duruyor, kullanıcıyı düzelteceği ekrana götüren bir yol yok.

### Y4 · Boş tamponda `##ATLA##` girilen adedi yakıyor

- [x] **KAPATILDI** (2026-09-04)

`matching.okut`, `komut == "atla"` dalında boş tampon kontrolü yok
(`##FAZLA##`'ya 2026-08-23'te eklenmiş, `##ATLA##`'ya eklenmemiş).

```
##ADET-150##   -> 150
##SONRAKI##    -> tip=bos     · adet 150 korunuyor   ✔
##FAZLA##      -> tip=bos     · adet 150 korunuyor   ✔
##ATLA##       -> tip=kuyruk  · kuyruk_id=None · adet 0   ✘
```

Hiçbir şey kuyruğa girmiyor, ekran "kuyruğa atıldı" diyor, 150 buharlaşıyor.

### Y5 · Çelişkili grupta girilen adet sessizce yanıyor

- [x] **KAPATILDI** (2026-09-04)

`matching._celiskili_grup` `bekleyen_adet`'i ne kullanıyor ne bildiriyor;
yanıtta `adet_yersiz` alanı yok. `grup_coz`'un diğer bütün dalları bu
sözleşmeyi tutuyor (`eslesti`, `slot`, `onay`, `kutu_acildi`).

```
##ADET-25## + iki farklı cihazın S/N'i + ##SONRAKI##
-> tip=coklu, sayi=2, adet_yersiz alanı YOK, bekleyen_adet 0
```

---

## ORTA

### O1 · `miktar_sigmiyor` ekrana ham etiket olarak düşüyor

- [x] **KAPATILDI** (2026-09-04)

`matching.fazla_bagla` özenle yazılmış bir `mesaj` döndürüyor
("Bu fazla kaydı %g adet, seçilen kayda %g adet sığıyor…"), ama
`routers/oturum.py:200` `HTTPException(400, sonuc["hata"])` ile onu atıyor.
`Esleme.tsx` `e.message`'ı olduğu gibi kırmızı kutuya basıyor:

```
bagla -> 400 | detail: "miktar_sigmiyor"
```

`routers/kuyruk.py` aynı durumu doğru yapıyor (`{"hata","mesaj"}`) — iki router
aynı sözleşmede değil.

### O2 · Kapalı oturumda kayıt değiştirilebiliyor

- [x] **KAPATILDI** (2026-09-04)

| Uç | Oturum kapalıyken |
|---|---|
| `POST /oturum/{id}/okut` | 409 ✔ |
| `POST /oturum/{id}/adet` · `/raf` · `/say` · `/sabit-kod` · `/kutu-kapat` · `/yedek-parca` | 409 ✔ |
| `DELETE /okutma/{id}` | **200 — siliyor** |
| `PATCH /okutma/{id}` | **200 — yazıyor** |
| `POST /oturum/{id}/gerial` | **200** |

Raporu üretilip Tiger'a girilmiş bir oturum sessizce değiştirilebilir.
`/okutma/{id}/bagla` ve `/coz-ayir` bilerek serbest kalmalı (eşleştirme sayım
sonu adımı), ama silme/geri alma öyle değil.

### O3 · `elle_say` lot satırında hep 1 yazıyor

- [x] **KAPATILDI** (2026-09-04)

`POST /oturum/{id}/say` ucunda `adet` parametresi yok, `matching.elle_say`
`miktar=1` sabit yazıyor. Elle sayma (I5) tam olarak barkodsuz — yani
dökme/lot — kalemler için yazılmıştı; 77'lik lotu bu yoldan saymak 77 ayrı
tıklama, 77 ayrı grup demek.

### O4 · `tip="bos"` için ekranda hiçbir mesaj yok

- [x] **KAPATILDI** (2026-09-04)

`Sayim.tsx` `seritMetni`'de `bos` için case yok, `default: return null`.
Boş tamponda F3'e basan kullanıcı yalnızca uyarı sesi duyuyor; ekranda hiçbir
şey değişmiyor, ne olduğu söylenmiyor.

### O5 · Aynı ürün kuyruğa iki kez düşebiliyor

- [x] **KAPATILDI** (2026-09-04)

Kap kayıtlarında koruma var (`_acik_kutu_kuyrugu`), normal kuyruk kaydında yok.

**Canlı veride:** `kuyruk#125` ve `kuyruk#126` birebir aynı
(`["MIC-75GF10-00A1", "KMAA700581"]`, 16:05 ve 16:07). İkisi de çözülürse tek
ürün iki kez sayılır.

---

## DÜŞÜK — basılı malzeme ve kullanım

### Dx1 · Komut kartındaki `##GERIAL##` etiketi yanlış

- [x] **KAPATILDI** (2026-09-04)

`app/barkod.py` `KOMUTLAR`:

```
("##GERIAL##", "SON OKUTMAYI SİL",
 "Yanlış okuttuğun tek barkodu geri alır."),
```

Gerçek davranış (`matching.gerial`): tampon doluysa tek barkod, **tampon boşsa
son GRUBUN tamamı** — `##ADET-5##` çok lotlu malzemede 5 satır — artı öğrenilen
barkodlar ve etiket bağlaması. CLAUDE.md §4.5 zaten "Son GRUBU geri al" diyor.

Kart laminatlı ve sahada elde taşınıyor: kullanıcı bir barkod sildiğini sanıp
ürünün tamamını siliyor.

### Dx2 · `##SONRAKI##` ve `##YEDEK##` kartları aynı renkte

- [x] **KAPATILDI** (2026-09-04)

`RENKLER` 9 renk, `KOMUTLAR` 10 komut, `RENKLER[i % len(RENKLER)]` →
i=0 (`##SONRAKI##`) ve i=9 (`##YEDEK##`) aynı koyu yeşil (`#1b5e20`).

En sık kullanılan komutla, açık unutulduğunda bütün rafı yedek parçaya yazan
mod anahtarı. Kötü depo ışığında renk, kartı bulmanın birincil yolu
(CLAUDE.md §10.1: "Renk tek başına bilgi taşımaz" — burada renk ayırt bile
etmiyor).

### Dx3 · `Escape` = `##IPTAL##`, onaysız ve geri alınamaz

- [x] **KAPATILDI** (2026-09-04) — onay eklendi

`Sayim.tsx` kısayol filtresi yazı alanlarında susuyor ama okuyucu kutusunu
bilerek muaf tutuyor — ve o kutunun odakta kalması dayatılıyor ("Okuyucu girişi
odakta değil" kırmızı bandı). Escape evrensel "bu kutuyu kapat" refleksidir;
tek tuş 20 barkodluk tamponu siliyor ve `##GERIAL##` bunu geri getirmiyor
(tampon satırları veritabanından silindi).

`Sayim.tsx:1199` kap adet kutusu Escape ile blur ediyor → odak okuyucuya döner
→ ikinci Escape grubu siler.

### Dx4 · `<a download>` içinde `<button>`

- [x] **KAPATILDI** (2026-09-04)

`Rapor.tsx:82` — geçersiz iç içe etkileşimli içerik. Çalışması tarayıcının
olay yayılımına bağlı; klavye ile odaklanan düğmenin kendi `onClick`'i yok.

### Dx5 · `textarea` 48 px dokunma hedefi kuralının dışında

- [x] **KAPATILDI** (2026-09-04)

`stil.css` kuralı `button, select, input, [role="button"]` kapsıyor.
Telefondaki not alanı `textarea` (`Telefon.tsx`). `input` bu kurala
2026-08-23'te tam olarak aynı sebeple eklenmişti.

### Dx6 · Oturum #1'de çözülmemiş kuyruk kaydı kalmış

- [ ] Veri düzeltmesi (kod değil)

`kuyruk#1` (`["DK-000001"]`, tanımsız kap, 10:12) açık; oturum #1 10:13'te
kapatılmış — yani `zorla` ile geçilmiş. `kutu` tablosunun boş kalmasının
sebeplerinden biri bu.

---

## Ne yapıldı (2026-09-04)

507 arka uç (+29) ve 32 arayüz testi geçiyor, `npm run build` temiz.
Regresyon testleri `tests/test_denetim_20260904.py`'de; K1'inkiler B2
ailesinin yanında, `tests/test_b1_barkod.py`'de. Her düzeltme, düzeltme
GERİ ALINDIĞINDA ilgili testin düştüğü doğrulanarak yazıldı.

| # | Düzeltme |
|---|---|
| K1 | `_yeni_seri`'de UPC ve kap elemesi kısa devrenin ÜSTÜNE alındı; `or parcalar` geri düşüşü kaldırıldı — aday kalmazsa öneri yok (`_sn_karar` ile aynı sözleşme) |
| K2 | `coz()` 1c'ye ikinci dal: `etiket.oturum` bu oturumsa `tekrar`. Yalnız DS- etiketine, yalnız bu oturuma özel |
| K3 | Kilit, grupta `fazla_bilinen` barkod varsa devreye girmiyor. Elle okutulan kod ikisini de yenmeye devam ediyor |
| Y1 | `SAYILMADI` sabiti `KAPASITE_VAR` ile değiştirildi; `BEKLENEN_ADET` tek yere (`matching.py` başı) indi. `slot` ve `coz()` 5 artık kapasite ölçütünde |
| Y2 | `oturumlar.gecmis` `sayaclar` ile aynı SQL'i kullanıyor; ekran `okutulan / toplam` gösteriyor |
| Y3 | `ApiHatasi.detay` eklendi; `App.oturumBitir` aşılabilirliği `detail` ANAHTARINDAN okuyor. Adsız/fotoğrafsız kapıları bilerek sert kaldı ama artık ne yapılacağını söylüyor |
| Y4 | `##ATLA##` boş tamponda `bos` dönüyor, hiçbir şey tüketmiyor |
| Y5 | `_celiskili_grup` `adet_yersiz` bildiriyor, şerit gösteriyor |
| O1 | `/bagla` ve `/say` hatayı `{hata, mesaj}` olarak veriyor (`routers/kuyruk.py` ile aynı) |
| O2 | `ortak.oturum_getir(acik=True)`; `gerial` · `PATCH` · `DELETE /okutma` · `seri-sec` kapalı oturumda 409. Eşleştirme uçları bilerek dışarıda |
| O3 | `/say` `adet` alıyor; verilmezse `bekleyen_adet` kullanılıp tüketiliyor. Kapasiteyi aşan adet reddediliyor, telefonda uyarı satırı |
| O4 | `tip="bos"` için şerit metni |
| O5 | `_ayni_kuyruk` — aynı barkod kümesi kuyrukta duruyorsa UYARIR; kayıt yine yazılır (iki fiziksel ürün olabilir) |
| Dx1 | Komut kartı: **GERİ AL** + gerçek davranışı anlatan açıklama |
| Dx2 | `RENKLER` 12'ye çıktı + `assert len(RENKLER) >= len(KOMUTLAR)` |
| Dx4 | `IndirBaglantisi` bileşeni — `<a download>` içinde `<button>` kalmadı |
| Dx5 | `textarea` 48 px kuralına dahil |

### Dx3 kararı: onay eklendi

Kullanıcı kararı (2026-09-04): **onay gelsin.** Uygulanışı üç kurala bağlandı,
çünkü yanlış uygulanmış bir onay hiç onay olmamasından kötüdür:

1. **Yalnızca kaybedilecek bir şey varken sorar.** Boş tamponda modal açmak
   maliyeti olmayan bir işleme engel koymaktır ve her gereksiz onay bir
   sonrakinin refleksle geçilmesini öğretir.
2. **Ne gideceğini yazar** — okutulan barkodların listesi ve varsa girilen
   adet. "Emin misiniz?" hiçbir şey söylemez.
3. **Komut barkodu bu onaya GİRMEZ.** Laminatlı karttan doğru olanı seçip
   okutmak zaten bilinçli bir eylem; üstelik açık bir modal okuyucunun yazdığı
   alanı odaktan düşürür, yani kartla iptal eden kullanıcı onayı okutamaz.
   Risk profili farklı: `Escape` bir refleks, telefondaki düğme eldivenle
   yanlış dokunuş, kart bir karar.

Kapsam: `Sayim.tsx` klavye kısayolu (`Escape`) ve `Telefon.tsx` İptal düğmesi.

### Açık kalanlar

**Canlı veri düzeltmeleri — kod değil, `sayim.db` içeriği.** Sayıma devam
etmeden önce elle yapılmalı:

| Ne | Nerede | Niçin |
|---|---|---|
| Okutma #92 miktarı 1 → 35 | Akış listesi, adet düğmesi | Adet, ad kutusuna yazılmış ("… 35 tane") |
| Okutma #22 · #92 seri no'su temizlensin | — | UPC taşıyorlar; K1 artık üretmiyor ama bu iki kayıt önceden yazıldı |
| Okutma #23 ya da #26'dan biri silinsin | Akış listesi, Sil | İkisi de `DS-000054`; tek cihaz, iki fazla adedi (K2) |
| `kuyruk#125` ya da `#126` | Kuyruk ekranı | Birebir aynı iki kayıt (O5) |
| `kuyruk#1` (oturum #1, `DK-000001`) | — | Kapalı oturumdan kalma açık kayıt; `kutu` tablosu bu yüzden boş |
| Okutma #22 · #23 adları | Akış listesi | "*Yanlış Okuma İgnorla" — Fazla sekmesine bu adla düşecek |
| `SL-75 A` / `SL-75A` | Akış listesi | Aynı ürün iki adla, raporda iki satır |

---

## Doğru bulunanlar

Denendi, sorun çıkmadı — bir daha bakılmasın:

- `norm()` · `sifirsiz()` · `raf_adi()` — Türkçe katlama, beyaz liste
- `upc_mi()` — UPC-A (12) ve EAN-13 (13) ağırlıkları ayrı ayrı doğru
- `kirli_mi()` — beş desen, `LICENSE` düzeltmesi yerinde
- `_like_kacir()` — `%` / `_` joker kaçışı, `ESCAPE '!'`
- `ara()` — parametre sırası `nere` ile birebir; `limit=0` sınırsız
- `_adet_dagit()` — çok lotlu dağıtım, artan miktar son paya
- `_geri_ekle()` — `geri` ekleniyor, ezilmiyor (D2 kapalı)
- `etiketler.klasor()` — `:memory:` için `None` (D5 kapalı)
- `db.bolunmus_fazlalari_birlestir()` · `lic_kuralini_duzelt()` — canlı veride no-op
- `importer` tekrar koruması — gerçek veride 0 yanlış eleme
  (870 satırda tekrar eden `(kod, ambar, seri)` üçlüsü yok)
- `oturumlar.bitir()` idempotent, ikinci çağrı zaman damgasını bozmuyor
- `POST /oturum` — açık oturum başka ambardaysa 409
- `Sayim.tsx seritMetni` — motorun 39 `tip` değerinin 36'sı ele alınmış
  (`tampon` ayrı yol, `bos` → O4, `kutu_seri` Kuyruk ekranında)
- Arayüzde emoji yok (CLAUDE.md §10.1 kuralı tutuyor)
- SSE yayını `status_code < 400` ile sınırlı — kapı yanıtları haber vermiyor
